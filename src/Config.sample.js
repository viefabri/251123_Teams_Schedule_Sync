/**
 * 【見本】プロジェクト設定
 * ※実際に使用する際は、このファイルを 'Config.js' にリネームし、
 * 以下の値を自身の環境に合わせて書き換えてください。
 */
const CONFIG = {
  // JSONファイルが格納されているフォルダのID
  FOLDER_ID: 'YOUR_FOLDER_ID_HERE', 
  
  // 読み込むファイル名
  FILE_NAME: 'schedule_sync.json',
  
  // 同期先のカレンダーID
  CALENDAR_ID: 'primary',
  
  // 同期範囲
  SYNC_RANGE_DAYS: 30
};